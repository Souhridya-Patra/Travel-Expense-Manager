PORT=8002
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/travel_expense_manager
JWT_SECRET=change-me-in-prod
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173,http://localhost:4173,http://127.0.0.1:4173

# Google token verification (must match frontend VITE_GOOGLE_CLIENT_ID)
GOOGLE_CLIENT_ID=428178433259-totcan3sf49k76b5kt42q8so76imbtfu.apps.googleusercontent.com

# OTP verification settings
OTP_EXPIRY_MINUTES=5

# SMTP (optional in local dev; if omitted, emails are logged with jsonTransport)
MAIL_FROM=saath807@gmail.com
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=saath807@gmail.com
MAIL_PASS=nvil nuih tsyk jcli
