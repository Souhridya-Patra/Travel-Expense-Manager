# Frontend Environment Variables

# ML Service URL - Backend ANI for receipt OCR and item parsing
# Local development (assuming ML service runs on port 8001)
VITE_ML_SERVICE_URL=http://localhost:8001

# Production (uncomment and update for production)
# VITE_ML_SERVICE_URL=https://api.yourapp.com/ml

# API Gateway URL for user auth and expense management
# Local development
VITE_API_GATEWAY_URL=http://localhost:8000

# Google Identity (Frontend Sign-In button)
# Must be a WEB OAuth Client ID from Google Cloud Console
# Example: 1234567890-abc123def456.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_ID=

# Optional Phase 3.2 receipt-storage context for frontend-only flow
# You can also set these in localStorage as authToken and activeTripId
# VITE_APP_AUTH_TOKEN=your_jwt_token
# VITE_ACTIVE_TRIP_ID=your_trip_id

# Production
# VITE_API_GATEWAY_URL=https://api.yourapp.com
